# KiraAI Phase 0 完整技术文档

> 给"以后的我"看的内部参考。覆盖 Phase 0（M1–M7 + 两次 amendment）已交付的全部框架基建：模型组、可编排 system prompt、in-chat 深度注入、Jinja2 翻译器、异步流水线、频率策略与观察者、输出钩子链。
>
> 阅读顺序建议：§0 总览 → §1–§5 各模块 → §6 横切关注 → §9 关键陷阱合集。每节末尾都标了"已知坑"，回坑时直接翻那里。
>
> 与文档的关系：
> - [`计划.md`](计划.md) — 战略蓝图（Phase 0–7 全图，wide）
> - [`Phase0计划.md`](Phase0计划.md) — Phase 0 设计书（design rationale，deep）
> - [`Phase0_M3_handoff.md`](Phase0_M3_handoff.md) — 阶段交接（execution log，by milestone）
> - **本文** — 完工后的完整技术参考（reference，by subsystem）

---

## 0. 总览

### 0.1 设计动机

原版 KiraAI 是 LLM ↔ 聊天平台 的连接层。要在它之上做**拟人化插件**（情绪栈 / 好感度 / 主动消息 / 弧光等）需要几个底层能力，原版都没有：

1. **多 key 自动 fallback**——白嫖模型限额很常见，调用方不应关心具体走哪个 key。
2. **可编排 system prompt**——情绪 / 偏差 / 记忆每轮可能要往 system 塞条目，"一整块 system prompt 字符串"无法管理。
3. **延迟一轮的资源准备**——表情包候选、记忆检索这种"额外 LLM/IO 工作"不能阻塞用户回复。
4. **输出层的钩子**——人化打字延迟、消息拆分、unsent（已读不回）都要在"LLM 输出 → 实际发送"之间动手。

Phase 0 全部用来填这四个洞，不动业务逻辑。所有改动**追加式**，老插件零迁移即可继续工作。

### 0.2 Phase 0 全部交付物

| 里程碑 | 主题 | 主要新增 | 测试文件 |
|---|---|---|---|
| **M1** | 模型组骨架 | `core/provider/model_group.py`、`KiraConfig.load_subconfig` | M2 包含 |
| **M2** | 模型组完整功能 | 错误分类 + 冷却 + capability + AgentExecutor 集成 | `tests/test_model_group_m2.py` |
| **M3** | PromptBlock + Assembler | `core/prompt_block.py`、`core/prompt_assembler.py`、`@register.prompt_block`、`EventType.ON_PROMPT_ASSEMBLE` | `tests/test_prompt_block_m3.py` |
| **M3 amendment** | In-chat 深度注入 | PromptBlock 加 `position/inject_depth/inject_role`；`ChatInjection`；`AssembledPrompt.chat_injections` | 同上（增 t13–t16） |
| **M4** | Translator + Jinja2 | `core/prompt_translator.py`、`core/prompts/translator/*.j2`、`data/config/translator.json` | `tests/test_translator_m4.py` |
| **M4 amendment** | RenderedPrompt 透传 chat_injections | RenderedPrompt 加字段 | 同上（增 t12） |
| **M5** | hints 异步流水线 + sticker 迁移 | `core/pipeline/hints_pipeline.py`、`@register.hints_handler`、`EventType.AFTER_LLM_RESPONSE_PARSE`、sticker 双路径 | `tests/test_hints_pipeline_m5.py` |
| **M5 amendment** | FrequencyPolicy + Observer | `FrequencyPolicy` / `HintsResult`、`set_policy` / `list_pending`、`EventType.ON_HINTS_PRODUCED`、`@on.hints_produced` | 同上（增 s16–s24） |
| **M7** | 输出钩子链 | `core/output/output_ctx.py`、`DefaultDelayHook`、`EventType.ON_OUTPUT_PIPELINE`、`@on.output_pipeline` | `tests/test_output_pipeline_m7.py` |
| **M7 顺带** | 旧侵入式修改回迁 | OpenAI v1/chat 生图、原生多模态（图/音/视频）、`desc_video` | M7 测试涵盖 |

**注意**：M6 计划中的"sticker 迁移"已合入 M5；M7 编号沿用计划。M8 仅是文档收尾（README + DEPRECATED 标注），不引入代码改动。

### 0.3 模块依赖图

```
                         ┌──────────────────────────┐
                         │    KiraLifecycle (启动)   │
                         └────┬─────────┬─────────┬─┘
                              │         │         │
            ┌─────────────────┘         │         └────────────────┐
            ▼                           ▼                          ▼
   ProviderManager → ModelGroupManager  PromptBlockRegistry     HintsPipeline
            │                           PromptAssembler         (M5)
            │                           PromptTranslator (M4)
            │                                  │
            └──────────────┬───────────────────┤
                           ▼                   ▼
                   AgentExecutor        MessageProcessor.handle_im_batch_message
                   (每步打 group)              │
                                               ├─ ON_LLM_REQUEST (legacy)
                                               ├─ ON_PROMPT_ASSEMBLE (M3)
                                               ├─ Assembler → Translator
                                               ├─ chat_injections insert (M3 amend)
                                               ├─ ModelGroup.call (M2)
                                               ├─ send_xml_messages
                                               │     ├─ ON_LLM_RESPONSE (legacy)
                                               │     ├─ AFTER_XML_PARSE (legacy)
                                               │     ├─ AFTER_LLM_RESPONSE_PARSE (M5)
                                               │     ├─ ON_OUTPUT_PIPELINE (M7) ← OutputCtx 钩子链
                                               │     └─ adapter.send_*
                                               └─ ON_STEP_RESULT (legacy)
                                                     │
                                                     └─ HintsPipeline.consume_response (M5)
                                                            │
                                                            ├─ FrequencyPolicy gate (M5 amend)
                                                            ├─ asyncio.create_task(prepare)
                                                            └─ ON_HINTS_PRODUCED fire-and-forget (M5 amend)
```

### 0.4 文件骨架（合并完成后）

```
core/
├── config/
│   ├── config_loader.py           # KiraConfig.load_subconfig / save_subconfig (M1)
│   └── default.py                  # 增加 default_llm_group / default_fast_llm_group
├── lifecycle.py                    # 实例化 ModelGroupManager / PromptBlockRegistry / PromptAssembler / PromptTranslator / HintsPipeline
├── llm_client.py                   # 加 model_group_mgr 字段
├── message_manager.py              # 主路径串起所有钩子（最重要的那个文件）
├── output/                         # (M7)
│   ├── __init__.py
│   ├── output_ctx.py
│   └── default_delay_hook.py
├── pipeline/                       # (M5)
│   ├── __init__.py
│   └── hints_pipeline.py
├── plugin/
│   ├── plugin_context.py           # 加 prompt_block_registry / hints_pipeline / model_group_mgr
│   ├── plugin_handlers.py          # EventType 加 ON_PROMPT_ASSEMBLE / AFTER_LLM_RESPONSE_PARSE / ON_OUTPUT_PIPELINE / ON_HINTS_PRODUCED
│   └── plugin_registry.py          # 装饰器集 + _register_plugin_*_for 系列
├── prompt_assembler.py             # (M3)
├── prompt_block.py                 # (M3 + amendment)
├── prompt_manager.py               # get_agent_prompt 改返回 list[PromptBlock]
├── prompt_translator.py            # (M4)
├── prompts/
│   ├── agent_tmpl.py               # DEPRECATED（保留兼容）
│   └── translator/                 # (M4)
│       ├── role.j2 / persona.j2 / attention.j2 / accounts.j2 /
│       ├── sessions.j2 / time.j2 / chat_env.j2 / memory.j2 /
│       ├── tools.j2 / output.j2 / format.j2
└── provider/
    ├── llm_model.py                # LLMRequest 字段标 DEPRECATED；多模态 list-content
    ├── model_group.py              # (M1+M2)
    └── src/openai/model_clients.py # 回迁：v1/chat 生图模式

data/config/                        # 项目惯例：每个新模块一个独立 JSON
├── model_groups.json               # (M1)
├── translator.json                 # (M4)
└── ...                              # Phase 1+ 自行追加

core/plugin/builtin_plugins/sticker/
├── main.py                         # 双路径 + StickerHintsKeyHandler (M5)
└── schema.json                     # use_hints_pipeline / hints_top_k / hints_policy_*

tests/
├── test_model_group_m2.py
├── test_prompt_block_m3.py
├── test_translator_m4.py           # 需 Jinja2>=3.1.0
├── test_hints_pipeline_m5.py
└── test_output_pipeline_m7.py
```

---

## 1. Phase 0.1 — 模型组（M1+M2）

### 1.1 设计意图

调用方（主对话、旁路、探针、日记）只持 `group_id`，不持 `key/url/model_id`。框架在 group 内按 priority 顺序尝试，遇到错误自动切换 + 冷却。

向后兼容关键：`models.default_llm` 单模型配置仍可用，框架内部包成虚拟 `__default__` 单成员组。**已部署的实例升级后零配置改动即可继续工作。**

### 1.2 数据结构（`core/provider/model_group.py`）

```python
@dataclass
class ModelEntry:
    ref: str                              # "<provider_id>:<model_id>" — 复用现有命名
    priority: int = 1                     # 数字小 = 越早尝试
    capabilities: list[str] = ...         # ["json_output","structured","long_context","creative",...]
    max_timeout: int = 30                 # 秒；超过视同失败

class ModelGroup:
    group_id: str
    entries: list[ModelEntry]
    cooldown_429: int = 300               # 默认 5 分钟
    cooldown_503: int = 1800              # 默认 30 分钟
    validate: dict = ...                  # min_chars / require_xml_root / require_json
    all_cooling_max_wait: int = 60       # 全部冷却时最多等多久

    async def call(self, request, prefer_capability=None) -> LLMResponse:
        ...

class ModelGroupManager:
    def __init__(self, provider_mgr, kira_config): ...
    def get_group(self, group_id) -> Optional[ModelGroup]: ...
```

**运行时状态**（不持久化，重启清零）：

```python
group_runtime[group_id] = {
    "model_state": {
        "<ref>": {
            "status": "ok" | "cooling",
            "cooldown_until_ts": int,
            "last_error": str,
            "last_used_ts": int,
        }
    }
}
```

### 1.3 错误分类与冷却（M2 升级）

| 触发 | 分类 | 冷却时长 | 切下一个 |
|---|---|---|---|
| HTTP 429 / "rate_limit" 字样 | rate_limit | 5 min | 立即 |
| HTTP 401/403 / "auth" / "key invalid" | auth | 30 min | 立即 |
| HTTP 5xx / 连接错误 / "unavailable" | unavailable | 30 min | 立即 |
| HTTP 400 类 / "invalid_request" | invalid_request | 不冷却 | 立即（可能是 prompt 问题） |
| `asyncio.wait_for` 超时 | timeout | 5 min（连续 3 次升级 30 min） | 立即 |
| 响应校验失败（min_chars / require_xml_root / require_json） | validate_fail | 不冷却（连续 3 次升级 30 min） | 立即 |
| 全部冷却 | — | 等最早过期的，超 `all_cooling_max_wait` 抛 `GroupExhaustedError` | — |

**切换日志**：每次切都打一行 WARN，便于事后排查：

```
[ModelGroup] sidecar_group: openai-deepseek:deepseek-chat → modelscope-qwen:qwen2.5-72b (reason: 429)
```

### 1.4 配置文件 `data/config/model_groups.json`

```json
{
  "groups": {
    "sidecar_group": {
      "models": [
        {
          "ref": "openai-deepseek:deepseek-chat",
          "priority": 1,
          "capabilities": ["json_output", "structured"],
          "max_timeout": 30
        },
        {
          "ref": "modelscope-qwen:qwen2.5-72b",
          "priority": 2,
          "capabilities": ["long_context", "creative"],
          "max_timeout": 45
        }
      ],
      "cooldown_429": 300,
      "cooldown_503": 1800,
      "validate": { "min_chars": 1, "require_xml_root": false }
    },
    "main_group": { "models": [...] }
  }
}
```

`KiraConfig.load_subconfig("model_groups", default={"groups":{}})` 自动在缺失时创建默认文件。

### 1.5 集成点

| 调用方 | 文件 / 入口 | 路径 |
|---|---|---|
| 主对话 | `core/message_manager.py` `handle_im_batch_message` ~454 行 | 先看 `models.default_llm_group` → 若空则用 `__default__` 虚拟组 → 仍空回退 `provider_mgr.get_default_llm()` |
| AgentExecutor 多步循环 | `core/agent/agent_executor.py` `run()` | `AgentExecutionContext.model_group` 字段；每 step 优先 `group.call()` 否则回退 `llm_model.chat()` |
| 插件 | `ctx.get_model_group(group_id)` (`core/plugin/plugin_context.py`) | 旁路 / 探针 / 日记自取 |
| WebUI | `webui/routes/providers.py` 等 | 后续接入；M2 范围只暴露 `list_models()` |

### 1.6 默认模式兼容

`ModelGroupManager` 启动时根据 `models.default_llm` / `models.default_fast_llm` / `models.default_vlm` 自动构造三个虚拟单成员组：`__default__` / `__default_fast__` / `__default_vlm__`。

旧调用 `provider_mgr.get_default_llm().chat(...)` 不变；新调用 `ctx.get_model_group("__default__").call(...)` 通向同一个底层 client。

### 1.7 自检

`tests/test_model_group_m2.py`：8 组场景共 27 个 check。覆盖 priority 选择、429 切换、超时升级、validate_fail 升级、capability 偏好、全冷却等待与超时、虚拟 `__default__`、并发安全。

### 1.8 已知坑

1. **`max_timeout` 触发 = 临时 5 min 冷却，不是 30 min**：连续 3 次同一模型超时才升级到 30 min。日志会写 `(reason: timeout, strikes: 2/3)`。
2. **响应校验失败默认不计冷却**：可能是 prompt 写差了，不是模型坏了。但同一模型连续 3 次校验失败会升级到 30 min 长冷却。
3. **`prefer_capability` 命中不到**：降级到忽略 capability 的全量列表，不抛错。日志写 `(downgraded: no entry has 'json_output')`。
4. **进程重启全部冷却清零**：是有意设计——重启等同新会话，没必要继承前一进程的冷却状态。Phase 0 不做持久化。

---

## 2. Phase 0.2 — PromptBlock + Assembler（M3 + in-chat amendment）

### 2.1 设计意图（两套语义，不要混淆）

借鉴 SillyTavern，但要分清两种独立的注入机制：

| 机制 | 含义 | 落点 | 字段 |
|---|---|---|---|
| **system 拖拽** | 多个系统级条目按排序键拼成 **单条 system prompt** | `request.messages[0]` 那条 system | `position="system"` + `depth` |
| **in-chat 深度注入** | 块作为独立 message 插入 **聊天历史** 指定深度 | `request.messages[len - inject_depth]` | `position="in_chat"` + `inject_depth` + `inject_role` |

**M3 落地时只有 system 拖拽语义。in-chat 注入是 amendment（post-M3）补的**——M3 原始的 `role="user"` 字段虽然存在但是 dead code，message_manager 只读 system_text，user_blocks 永远到不了 LLM。

### 2.2 PromptBlock 字段（`core/prompt_block.py`）

```python
@dataclass
class PromptBlock:
    name: str                                              # 块标识（同 plugin 内唯一）
    content_provider: ContentProvider                      # 同步或异步；可零参或接 ctx_snapshot
    depth: int = 100                                       # 系统块排序键（小 = 靠前）
    enabled: bool = True                                   # 静态开关
    role: Literal["system", "user"] = "system"             # DEPRECATED — 见 position
    condition: Optional[ConditionFn] = None                # 运行时条件，返回 False 跳过
    cache_key: Optional[str] = None                        # 同一次 assemble 内去重缓存
    source: str = "framework"                              # "framework" | "plugin:<plugin_id>"
    # post-M3 amendment：SillyTavern 双轴
    position: Literal["system", "in_chat"] = "system"
    inject_depth: int = 0                                  # in_chat 时从 messages 末尾倒数
    inject_role: Literal["system","user","assistant"] = "system"
```

**字段优先级**：

- `position="system"`（默认）→ 走 system 拖拽路径，`depth` 排序，`role` / `inject_*` 忽略
- `position="in_chat"` → 走 in-chat 注入路径，`inject_depth` / `inject_role` 生效，`depth` 忽略
- `role="user"` 但 `position` 默认 `"system"`：合法但**到不了 LLM**（落 user_blocks 桶但被 message_manager 丢弃）。新代码用 `position="in_chat"` 替代

### 2.3 三个来源

```python
# 1. 框架内置（11 块，深度 10/20/.../110）
#    PromptManager.get_agent_prompt(chat_env) → list[PromptBlock]
#    role / persona / attention / accounts / sessions / time / chat_env / memory / tools / output / format

# 2. 插件静态（启动时一次注册）
@register.prompt_block(name="emotion_snapshot", depth=70, role="system")
def emotion_block(self, ctx):
    return f"角色当前情绪：{self.emotion_stack.snapshot()}"

# 3. 插件运行时（每轮重新评估）
@on.prompt_assemble()
async def per_turn_bias(self, event, ctx_snapshot, block_collector):
    if self.should_inject_bias(ctx_snapshot):
        block_collector.add(PromptBlock(name="bias", depth=72, content_provider=...))
```

`PromptBlockRegistry` 按 `plugin_id` 索引，`clear_plugin(pid)` 一次性清除某插件的所有静态块（plugin 终止/卸载时调用）。

### 2.4 AssembledPrompt 结构

```python
@dataclass
class AssembledPrompt:
    system_blocks: list[AssembledBlock]                    # → 拼 system prompt
    user_blocks: list[AssembledBlock]                      # DEPRECATED
    chat_injections: list[ChatInjection]                   # post-amendment 新增
    debug_meta: list[dict]                                 # /debug 用：{name, depth, source, ms, cache_hit, error?}

@dataclass
class ChatInjection:
    name: str
    content: str
    inject_depth: int                                      # 0 = 末尾, N = 倒数第 N 条之前
    inject_role: str                                       # "system" | "user" | "assistant"
    source: str
```

### 2.5 Assembler 工作流（`core/prompt_assembler.py`）

```
collect (3 个来源) → filter (enabled & condition) → sort by depth (stable) → evaluate content_provider
                                                                              ↓
                                            ┌─────────────────────────────────┴──────────────────────────────────┐
                                            ▼                                                                    ▼
                                   position="in_chat" → ChatInjection 入 chat_injections                position="system" → AssembledBlock 入 system_blocks
                                                                                                                 (旧 role="user" 仍走 user_blocks 桶但被丢弃)
```

**容错原则**：单块异常只 warn 不抛；返回的 PromptBlock 列表只少一条不影响其它块。`debug_meta` 始终记录每块的 `ms` / `cache_hit` / `error`。

`cache_key`：同一次 `assemble()` 调用内，相同 `cache_key` 的块复用第一次求值结果（避免 emotion-driven retrieval 在多个块里重复跑）。

### 2.6 In-chat 注入的执行点（关键时序）

`core/message_manager.py::handle_im_batch_message`：

```python
request.assemble_prompt()    # 把 user_prompt 拼成最后一条 user message 追加到 messages

# ── 在此之后 insert chat_injections ──
original_len = len(request.messages)
positioned = [
    (max(0, original_len - inj.inject_depth), idx, inj)
    for idx, inj in enumerate(assembled.chat_injections)
]
positioned.sort(key=lambda t: (-t[0], -t[1]))   # 大位置先插 + 同位置后注册先插
for pos, _idx, inj in positioned:
    request.messages.insert(pos, {"role": inj.inject_role, "content": inj.content})
```

**为什么必须在 assemble_prompt() 之后**：assemble_prompt() 会追加 user 消息到 messages 末尾。如果在它**之前** insert，新 inject 会被这次追加挤位（depth=0 的反而跑到 user 消息之前）。

**深度语义参考表**（假设 messages 长度为 6）：

| `inject_depth` | 落点（assemble_prompt 后） | 适合 |
|---|---|---|
| 0 | `messages[-1]` 之后（末尾） | 紧贴生成位的 reminder |
| 1 | 最新 user 消息之前 | 最常用：让 LLM 看到注入后立刻看到用户输入 |
| 4 | 倒数第 4 条之前 | World Info / 长生命周期语境 |

### 2.7 兼容策略

- 旧 `@on.llm_request` 改 `request.system_prompt` / `user_prompt`：**仍生效**。Assembler 在 `ON_LLM_REQUEST` 之**后**跑，把产出文本作为 `request.system_prompt[0]` 插入；老插件 `.append()` 的 Prompt 落在后面（追加策略）。
- `LLMRequest.assemble_prompt()` / `LLMRequest.user_prompt` / `LLMRequest.system_prompt` 全部加了 `# DEPRECATED(Phase 0 → Phase 1)` 注释，保留至 Phase 1 收尾。

### 2.8 自检

`tests/test_prompt_block_m3.py`：16 个场景。覆盖空块列表、同步/异步混存、condition 过滤、depth 排序、cache_key 命中、provider 异常隔离、`<|message_types|>` 占位符替换（M3 有 / M4 删）、registry 基本面、零参 provider、role="user" 路由（DEPRECATED 回归点）、in-chat 路由 / 显式 inject_role / 不污染 system_blocks / insertion 顺序。

### 2.9 已知坑

1. **`role="user"` + `position="system"` = dead 块**：内容到不了 LLM。两个字段同时设的话，position 优先；想真把内容送到 LLM 用 `position="in_chat"`。
2. **`__assembled__` 命名 Prompt**：M3 把 Assembler 产出包成单个 Prompt 插入 `request.system_prompt[0]`，名字固定 `"__assembled__"`。如果未来 ON_LLM_REQUEST 钩子需要按名字找它，要小心。
3. **Assembler 的 `cache_key` 只在单次 assemble 内有效**：跨轮缓存 ≠ 这个 key 的设计目的。要跨轮持久化用变量框架（Phase 1）。
4. **degraded path 不消费 in_chat**：`PromptManager.assembler` 没注入时（早期 boot / 测试 stub）走每块 to_legacy_prompt 的 fallback，**所有 in_chat 块在 degraded path 下静默丢弃**。这是有意设计：degraded 模式只保证基础可用。
5. **stable sort 的副作用**：`depth` 相同的两块按注册顺序排列。框架内置 11 块占了 10/20/.../110，留出 5/15/.../95 给插件。**`95` 已被 skills 占用**（`message_manager.py` `~450` 行）。

---

## 3. Phase 0.3 — Translator + Jinja2（M4 + amendment）

### 3.1 设计意图

Assembler 负责"哪些块、什么顺序、内容是什么"（**对代码友好的结构**）；Translator 负责"怎么拼成 LLM 能吃的 XML 文本"（**对 LLM 友好的字符串**）。换 prompt 协议（XML → JSON / Markdown / Anthropic 风格）只动 Translator + 模板，不碰插件和 Assembler。

### 3.2 模板组织（`core/prompts/translator/*.j2`）

11 个模板，从原 `agent_tmpl.py` 字面迁移，占位符 `{x}` 改 `{{ x }}`、`<|message_types|>` 改 `{{ message_types }}`：

```
role.j2       — 角色定位
persona.j2    — 人设内容
attention.j2  — 注意事项
accounts.j2   — 账号列表
sessions.j2   — 当前会话
time.j2       — 时间
chat_env.j2   — 聊天环境（platform/adapter/chat_type/...）
memory.j2     — 记忆段
tools.j2      — 工具说明（占位，实际工具走 OpenAI tools 字段）
output.j2     — 输出规约（max_tool_loop 等）
format.j2     — XML 标签规范 + hints_key 用法（M5 追加）
```

### 3.3 Jinja2 配置（`core/prompt_translator.py::PromptTranslator.__init__`）

```python
self._env = jinja2.Environment(
    loader=jinja2.FileSystemLoader(str(self.template_dir)),
    keep_trailing_newline=True,    # 模板尾部换行保留
    autoescape=False,              # <msg> 原样输出，不要 &lt;
    undefined=jinja2.ChainableUndefined,  # {{ chat_env.missing }} → "" 而非异常
)
```

### 3.4 渲染流程

```
PromptManager.get_agent_prompt(chat_env):
    返回 list[PromptBlock]，每块的 content_provider 是
        lambda _ctx, _name=name: translator.render_template(_name, _ctx)
    （Assembler 求值时间接驱动 Jinja2 渲染）

PromptAssembler.assemble(blocks, ctx):
    依次跑 content_provider → 各块拿到 j2 渲染结果
    输出 AssembledPrompt（system_blocks 已含渲染后文本）

PromptTranslator.render(assembled, tag_set, ctx) → RenderedPrompt:
    sys_parts = []
    for blk in assembled.system_blocks:
        if debug_markers: sys_parts.append(f"<!-- block:{name} depth=N source=... -->\n")
        sys_parts.append(blk.content)                  # j2 已渲染
        if debug_markers: sys_parts.append(f"<!-- /block:{name} -->\n")
    return RenderedPrompt(
        system_text="".join(sys_parts),
        user_text="".join(usr_parts),                   # DEPRECATED
        chat_injections=list(assembled.chat_injections),# post-amendment 透传
        debug_meta=list(assembled.debug_meta),
    )
```

### 3.5 配置 `data/config/translator.json`

```json
{
  "template_dir": "",         // 空字符串 = 用打包的 core/prompts/translator/
  "debug_markers": false      // true 时每块前后包 <!-- block:... --> 注释
}
```

`debug_markers=true` 仅供 `/debug prompt <sid>` 调试，会增加 token 数（LLM 通常容忍 XML 注释，但生产环境关掉）。

### 3.6 `<|message_types|>` 占位符

M3 时仍是字符串 replace（`message_manager.py:485-487` 老代码）。M4 起：

- `ctx_snapshot["message_types"] = tag_set.to_prompt()` 在 message_manager 里预算
- `format.j2` 用 `{{ message_types }}` 取
- M3 的 `str.replace` hack **删除**

**约束**：在 `ON_PROMPT_ASSEMBLE` 钩子里注册的 tag**不会**出现在 `message_types` 里——by design，tag 注册属于 `ON_LLM_REQUEST` 阶段（ON_PROMPT_ASSEMBLE 是 prompt 块阶段，不是 tag 阶段）。

### 3.7 自检

`tests/test_translator_m4.py`：12 场景。覆盖单模板渲染、嵌套 chat_env、缺失变量静默、`{{ message_types }}` 注入、缺失模板返回空、render 顺序保持、debug_markers 开关、autoescape 关闭、keep_trailing_newline、`list_templates()` 返回 11 个、user_blocks 桶（DEPRECATED 回归）、chat_injections 透传（amendment 新增）。

**依赖**：`pip install Jinja2>=3.1.0`，`requirements.txt` 已写。

### 3.8 已知坑

1. **模板里禁用 `{% include %}` 跨文件依赖**：每个模板独立。要复用片段，先在 message_manager 里 `render_template` 拿到字符串再传给下一个模板的 ctx。
2. **`debug_markers` 影响 token 数**：每块加两行注释，长 prompt 涨几十 token。生产环境务必关闭。
3. **降级路径仍跑 M3 字符串 replace**：`PromptTranslator` 没注入时 `message_manager` fallback 到 M3 行为（含 `<|message_types|>` replace）。这是为了 import 失败 / Jinja2 未装时的可用性。

---

## 4. Phase 0.4 — Hints Pipeline（M5 + FrequencyPolicy/Observer amendment）

### 4.1 设计意图（两轮流水线）

把"轮 N 顺手吐 key → 后台异步准备 → 轮 N+1 注入"的模式从 sticker 单点提到框架级。任何"需要异步准备的资源"（表情包、记忆检索、世界书查询、emotion-driven retrieval）共享同一套接口。

```
轮 N:     LLM 输出 ... <hints_key type="sticker">happy,thumbs_up,wow</hints_key> ...
   │
   ▼ message_manager.send_llm_text 调
   HintsPipeline.consume_response(sid, llm_response, ctx_snapshot)
   │
   ├─ 解析 <hints_key type="X">k1,k2</hints_key>
   ├─ FrequencyPolicy gate (every / interval / random)
   │     ├─ 通过 → asyncio.create_task(handler.prepare(...))
   │     └─ 跳过 → fire ON_HINTS_PRODUCED with skipped_by_policy=True
   └─ task 在后台跑（不阻塞用户回复）
       │
       └─ 完成时 fire ON_HINTS_PRODUCED with HintsResult
   
轮 N+1:  message_manager 在 Assembler 求值前调
   HintsPipeline.collect_blocks(sid, max_wait_ms=200)
   │
   ├─ 遍历 pending 桶
   ├─ 已完成 + 未过 TTL → 取出 PromptBlock
   ├─ 未完成 → 等最多 200ms（不阻塞主路径）
   └─ TTL 超时 → 静默丢弃
   
   返回 list[PromptBlock] → 加入 all_blocks → Assembler 照常处理
```

### 4.2 HintsKeyHandler ABC

```python
class HintsKeyHandler(ABC):
    key_type: str = ""              # 必须，对应 <hints_key type="...">
    ttl_seconds: int = 600          # 块过期时间

    @abstractmethod
    async def prepare(self, keys: list[str], session_id: str, ctx: dict) -> Optional[PromptBlock]:
        ...
```

返回 `None` = "查到/合成什么也没有"，next turn 无注入。返回非 PromptBlock 对象会被 warn 后丢弃。

### 4.3 HintsPipeline 数据结构（`core/pipeline/hints_pipeline.py`）

```python
class HintsPipeline:
    self._pending: dict[sid, dict[key_type, _PendingEntry]]      # in-flight 任务
    self._handlers: dict[key_type, HintsKeyHandler]              # 已注册 handler
    self._by_plugin: dict[plugin_id, set[key_type]]              # plugin → 持有的 types
    # post-M5 amendment:
    self._policies: dict[key_type, FrequencyPolicy]
    self._policy_state: dict[sid, dict[key_type, _PolicyState]]
    self._handler_to_plugin: dict[key_type, plugin_id]
```

### 4.4 流水线生命周期接口

| 方法 | 用途 | 调用方 |
|---|---|---|
| `register(handler, plugin_id, policy=None)` | 注册 handler；policy 可选 | plugin_registry / 测试 |
| `unregister(key_type)` | 注销；in-flight task cancel | unregister 装饰器 |
| `clear_plugin(plugin_id)` | 清除某插件全部贡献 | plugin terminate |
| `clear_session(sid)` | 清除某 session 全部 pending | session 休眠（Phase 1 接） |
| `consume_response(sid, llm_response, ctx)` | 解析 + 调度 | message_manager |
| `collect_blocks(sid, max_wait_ms=200)` | 下一轮注入前调 | message_manager |
| `set_policy(key_type, policy)` (amend) | 运行时改策略，重置计数 | 调试/插件 |
| `get_policy(key_type)` (amend) | 查策略 | 调试 |
| `list_pending(sid)` (amend) | 当前 pending 只读快照 | /debug |
| `list_handlers()` | 已注册 type 列表 | /debug |
| `has_pending(sid)` | 简单 bool 查询 | 测试 |

### 4.5 关键约束（不可违背）

1. **prepare 必须不阻塞用户回复**：`asyncio.create_task` 派发，立即返回。
2. **同 type 同 session 重发 → 覆盖**：旧 in-flight task 被 cancel，最后一次 emission 胜出。
3. **跨 session 隔离**：`pending[sid_A]` 和 `pending[sid_B]` 互不可见。
4. **TTL 老化**：`now - created_ts > handler.ttl_seconds` 的 entry 在 collect_blocks 时静默丢弃。
5. **200ms backpressure**：collect_blocks 最多等 200ms，超时不阻塞主路径，未完成 task 沉默丢弃（已经从 bucket 拿走，所以不会污染下一轮）。
6. **未注册 type 静默忽略**：LLM 可能幻觉出 type 名，框架不报错。
7. **handler 异常隔离**：prepare 抛异常 → 写一行 warn，pending 不写入，不影响主路径。
8. **Cancellation 不触发观察者**：被 overwrite 取消的 task 不 fire ON_HINTS_PRODUCED——那是内部清理，不是真实结果。

### 4.6 `<hints_key>` 标签解析

正则（permissive）：

```python
_HINTS_KEY_RE = re.compile(
    r"<hints_key\s+type\s*=\s*[\"']([^\"']+)[\"']\s*>(.*?)</hints_key>",
    re.IGNORECASE | re.DOTALL,
)
```

**为什么不用 ElementTree**：LLM 输出可能没有单一根元素、`<hints_key>` 刻意放在 `<msg>` 之外、属性引号可能单可能双、可能带空白。

**多次合并语义**（决策 #2）：同一 type 多次出现 → keys 用 `set` 合并去重，但 handler 收到的是 `list[str]`（保留首次出现顺序）——确定性优先。

### 4.7 FrequencyPolicy（amendment）

```python
@dataclass
class FrequencyPolicy:
    mode: Literal["every", "interval", "random"] = "every"
    interval: int = 1            # mode="interval": 第 N、2N、3N… 次触发
    probability: float = 1.0     # mode="random": 每次 P 概率触发
    seed: Optional[int] = None   # 可选，用于复现
```

**计数语义**：counter per (sid, key_type)，从 1 起算。
- `every`：永远 True
- `interval`：`count % interval == 0` 时 True
- `random`：`rng.random() < probability`

**Skip 路径触发观察者**：策略跳过的 emission 仍 fire `ON_HINTS_PRODUCED`，`skipped_by_policy=True`，便于 /debug 看 skip 速率。

**`set_policy` 重置计数器**：切策略时该 type 在所有 session 的 counter 清零，避免新策略沿用旧节奏。

### 4.8 ON_HINTS_PRODUCED 观察者（amendment）

```python
@dataclass
class HintsResult:
    session_id: str
    key_type: str
    keys: list[str]
    block: Optional[PromptBlock]   # 产出的块；None 表示 skip / 异常 / handler 返回 None
    error: Optional[str]            # 异常信息字符串
    elapsed_ms: float
    skipped_by_policy: bool
    plugin_id: Optional[str]        # handler 所属插件（observer 可识别来源）

# 装饰器
@on.hints_produced(priority=Priority.MEDIUM)
async def observer(self, event, hints_result):
    ...
```

**派发方式**：每个观察者用 `asyncio.create_task` 启动（**fire-and-forget**），慢观察者不阻塞用户回复路径；多个观察者间无序。需要严格顺序的下游链请在单个观察者里 await 串起来。

**不触发的情况**：被 overwrite 取消的 in-flight prepare（cancellation 是内部清理）。其他三种情况都触发：成功（block 非 None）、handler 返回 None（block=None, error=None）、handler 抛异常（block=None, error=str）、policy skip（block=None, skipped_by_policy=True）。

### 4.9 装饰器 + plugin_registry 集成

```python
@register.hints_handler(
    key_type="sticker",
    ttl_seconds=900,             # 可选 override
    mode="every",                # FrequencyPolicy 字段，可选
    interval=1,
    probability=1.0,
    seed=None,
)
def factory(self) -> Optional[HintsKeyHandler]:
    if not self.use_hints_pipeline:
        return None              # 工厂可在运行时 opt out
    return MyHandler(self.ctx)
```

**FrequencyPolicy 优先级**（高 → 低）：
1. handler 实例上的 `_frequency_policy` 属性（运行期 / config 驱动）
2. 装饰器 `mode/interval/probability/seed` 参数（静态 / 作者意图）

**约定**：要么用装饰器，要么用 `_frequency_policy`，**二选一**。两边都设会让"为什么我的策略没生效"变成排查噩梦。

### 4.10 Sticker 插件双路径

`use_hints_pipeline` 配置项（默认 `False`）切换：

| 路径 | tag description | 调度 |
|---|---|---|
| `False` (legacy) | 全部 sticker 列在 prompt 里 | `ON_LLM_REQUEST` 注入 TagSet description |
| `True` (pipeline) | 只描述用法 + `<hints_key type="sticker">` | LLM 吐 key → 后台 top-k 检索 → 下一轮注入候选 |

`hints_top_k` 控制每轮注入的候选数；匹配策略是简单的关键词子串 + hits 计数排序（不调 LLM / embedding，cheap）。匹配数为 0 时返回一个 diagnostic block "未匹配到 sticker，请换关键词"，避免 LLM 反复试同个无效 key。

### 4.11 Sticker 配置（`schema.json`）

```json
{
  "use_hints_pipeline": { "type": "boolean", "default": false },
  "hints_top_k":        { "type": "integer", "default": 12 },
  "hints_policy_mode":  { "type": "string",  "default": "every" },
  "hints_policy_interval":   { "type": "integer", "default": 1 },
  "hints_policy_probability": { "type": "number",  "default": 1.0 }
}
```

`_make_sticker_hints_handler` 工厂读 config，attach `_frequency_policy` 到 handler 实例后返回。

### 4.12 自检

`tests/test_hints_pipeline_m5.py`：24 场景（s1–s15 原始 + s16–s24 amendment）。

**M5 原始**（s1–s15）：空 hints_key、单 type 单 key、同 type 合并保序、多 type 互相独立、慢 prepare 200ms 后跳过、prepare 异常隔离、TTL 老化、覆盖式更新、session 隔离、未注册 type 忽略、None 结果、clear_session、unregister 取消、clear_plugin、属性引号空白容忍。

**Amendment**（s16–s24）：every 默认无跳、interval=3 仅第 3/6 次、random+seed 复现、probability=0 永不触发、set_policy 重置计数、observer 在 success 触发、observer 在 skip 触发、observer 在 exception 触发、list_pending 只读快照。

### 4.13 已知坑

1. **`<hints_key>` 必须放 `<msg>` 之外**：放在 `<msg>` 内会被 XML 解析当成消息内容尝试发给用户。format.j2 已写明白。
2. **counter 仅在内存**：进程重启清零。这是有意设计；要跨重启稳定的 interval 计数请走 Phase 1+ 的变量框架。
3. **observer fire-and-forget 与生命周期**：进程关停时未完成的 observer task 被 cancel。副作用必须持久化的 observer 要 try/except CancelledError 自己 handle。
4. **handler `_frequency_policy` 与装饰器不要双设**：见 §4.9。
5. **kira-ai 的 XML 修复钩子优先级**：HintsPipeline 必须在 XML 修复**之后**跑。新加的 `AFTER_LLM_RESPONSE_PARSE` 事件就是为了避免和 ON_LLM_RESPONSE 抢位。装饰器优先级写明 `Priority.SYS_LOW`，**勿改**。
6. **collect_blocks 等 200ms 后 task 不一定 cancel**：bucket 已 pop，但 task 自己继续跑——Python 会在它完成后 GC。如果你不希望孤儿 task，handler 内部用 `asyncio.timeout` 自己卡时间。

---

## 5. Phase 0.5 — 输出后处理钩子链（M7）

### 5.1 设计意图

LLM 原始输出 → XML 解析 → 实际发送 之间开放一组钩子，允许：

- **修改**消息内容（chains）
- **设置**每条消息发送前的延迟（delays）
- **拆分**单条长消息成多条 / 合并多条短消息成单条
- **拦截**整批消息（intercepted=True，标记 unsent）
- 通过 **`meta`** 跨钩子通信

### 5.2 OutputCtx 数据结构（`core/output/output_ctx.py`）

```python
@dataclass
class OutputCtx:
    event: KiraMessageBatchEvent                # 原始事件
    raw_text: str                                # LLM 原始输出（修复后）
    chains: list[MessageChain]                  # 解析后可变 chain 列表
    delays: list[Optional[float]]               # 每条消息前延迟秒数；None = 让 DefaultDelayHook 填默认
    intercepted: bool = False                   # True → 不发送
    unsent_reason: Optional[str] = None         # 拦截原因（写日志用）
    budget_ms: int = 0                          # 从 event.timestamp 起算的延迟预算余量
    api_elapsed_ms: int = 0                     # 本次 LLM 调用真实耗时
    meta: dict = field(default_factory=dict)    # 钩子之间共享笔记
```

### 5.3 时序

```
LLM 输出 → kira-ai 修复 XML (ON_LLM_RESPONSE)
         → _parse_xml_msg → list[MessageChain] (AFTER_XML_PARSE)
         → AFTER_LLM_RESPONSE_PARSE (M5: HintsPipeline 等挂这里)
         → 构造 OutputCtx (含实测 budget_ms / api_elapsed_ms)
         → ON_OUTPUT_PIPELINE 钩子链（按 priority 降序）
              ├─ 用户钩子（修改 chains/delays/intercepted）
              ├─ ...
              └─ DefaultDelayHook (SYS_LOW: 填空 delay 槽位)
         → 按 ctx.intercepted / ctx.chains / ctx.delays 决策
              ├─ intercepted=True → 跳过 send，仍触发 ON_STEP_RESULT (空 message_results)
              └─ 否则逐条 send_message_chain，按 delays sleep
```

### 5.4 装饰器

```python
@on.output_pipeline(priority=Priority.HIGH)
async def patience_hook(self, event, ctx: OutputCtx) -> None:
    if self.user_patience(ctx) < 0.3:
        ctx.chains = self.split_long(ctx.chains)
        ctx.delays = [0.5] * len(ctx.chains)
```

### 5.5 内置 DefaultDelayHook

```python
@on.output_pipeline(priority=Priority.SYS_LOW)
async def _default_delay_hook(event, ctx):
    for i, d in enumerate(ctx.delays):
        if d is None:
            ctx.delays[i] = random.uniform(min_delay, max_delay)
```

行为与原版 `random.uniform(min_message_delay, max_message_delay)` 一致——把现有 `message_manager.py:631-632` 的逻辑包成一个钩子，便于后续 Phase 5 替换。

`min_message_delay` / `max_message_delay` 仍走 `bot_config.bot.*` 老配置；M7 引入 `data/config/output_pipeline.json` 备用，但当前未必需要。

### 5.6 阻塞 vs 非阻塞约定

- **修改 chains / 决定 delay**：阻塞，钩子必须在发送前跑完。
- **重型副作用**（写日志 / 更新统计 / 推 memory）：钩子内 `asyncio.create_task(...)` 自己异步，**不**阻塞链。
- **session_lock**：链整体在锁内执行。**delay > 5s 不要用 sleep**——会阻塞同 session 后续消息。M7 不支持长 delay 钩子，留给 Phase 5（用独立 scheduler）。

### 5.7 拦截语义

`ctx.intercepted=True`：
- 框架不发送任何 chain
- **仍**触发 `ON_STEP_RESULT`，但 `step_result.message_results` 是空 list
- 这样 Phase 5 的 unsent 机制能正确记录"想说但没说"
- 写日志 / 入 unsent 表是钩子自己的事（M7 只搭骨架）

### 5.8 budget_ms / api_elapsed_ms 实测

- `budget_ms = event.timestamp_to_ms - now_ms`（用户消息时刻起算）
- `api_elapsed_ms = sum(per_step_chat_durations)`（在 AgentExecutor 每 step 测量后累加）

为 Phase 5 "延迟预算制" 准备：API 已经卡了 8s 就不再额外加打字延迟，避免本来就慢还要装一会的体验。

### 5.9 自检

`tests/test_output_pipeline_m7.py`：覆盖默认 delay 行为不变、钩子修改 delays 生效、修改 chains 生效、intercepted 不发但 ON_STEP_RESULT 触发、budget_ms / api_elapsed_ms 已填、调试钩子读 chain 长度等。

### 5.10 已知坑

1. **链整体在 session_lock 内**：写慢副作用务必 `asyncio.create_task`。
2. **chains 长度 vs delays 长度对齐**：钩子可以拆/合并 chains，但 `delays` 要保持等长。框架在调 `send_message_chain` 之前做一次长度对齐——缺的位置补 None（让 DefaultDelayHook 填）。
3. **DefaultDelayHook 的可关闭性**：lifecycle 启动时注册的"内置钩子"用 SYS_LOW，能被插件抢占（更高优先级先跑）。但**它不会覆盖更高优先级钩子设的 delay**——只在槽位是 None 时填。
4. **拦截后的 ON_STEP_RESULT 处理**：现有 ON_STEP_RESULT handler（如 message id 替换）需能容忍 `message_results=[]`。

### 5.11 M7 顺带回迁的旧侵入式修改

M7 同时承载三项 v2.x 之前对原版的侵入式增强：

- **OpenAI Image Client v1/chat 模式**（`core/provider/src/openai/model_clients.py`）：类属性 `USE_CHAT_COMPLETIONS=True` 切换到 `/v1/chat/completions` 多模态聊天接口出图。从 Markdown `![alt](url)`、`data:image/` URI、`output_image` 内容部件三种形式提取图片，**明确拒绝从纯文本里抽 URL**（防注入）。
- **原生多模态**（`core/provider/llm_model.py`）：图 / 音 / 视频 base64 直喂支持视觉的 LLM，跳过 VLM 描述。三个开关独立：
  - `bot_config.bot.use_native_multimodal`（图）
  - `bot_config.bot.use_native_audio_multimodal`（音）
  - `bot_config.bot.use_native_video_multimodal`（视频）
  默认全 `False`，走原 VLM 描述+缓存路径。打开后 `LLMRequest` user message 自动从 `string content` 切到 OpenAI vision 的 `list-content` 形态。
- **`desc_video` 工具函数**（`core/utils/common_utils.py`）：跟 `desc_img` 同位置，给"视频不走原生多模态但仍想要描述"的场景。

---

## 6. 横切关注点

### 6.1 EventType 全集（Phase 0 后状态）

| 事件 | 触发点 | 处理器签名 | 来源 |
|---|---|---|---|
| `ON_IM_MESSAGE` | 消息到达 | `(event)` | legacy |
| `ON_MESSAGE_BUFFERED` | 消息进缓冲区 | `(sid)` | legacy |
| `ON_IM_BATCH_MESSAGE` | 消息合并 | `(event)` | legacy |
| `ON_LLM_REQUEST` | LLM 请求前（DEPRECATED 主用法） | `(event, request, tag_set)` | legacy |
| `ON_PROMPT_ASSEMBLE` | LLM 请求前（推荐主用法） | `(event, ctx_snapshot, block_collector)` | **M3** |
| `ON_LLM_RESPONSE` | LLM 原始输出 | `(event, llm_response)` | legacy |
| `AFTER_LLM_RESPONSE_PARSE` | XML 修复后 | `(event, llm_response, raw_xml, message_chains)` | **M5** |
| `AFTER_XML_PARSE` | XML 解析后 | `(event, message_chains)` | legacy |
| `ON_OUTPUT_PIPELINE` | 输出链 | `(event, ctx: OutputCtx)` | **M7** |
| `ON_TOOL_RESULT` | 工具调用结果 | `(event, tool_result)` | legacy |
| `ON_STEP_RESULT` | Agent step 结果 | `(event, step_result)` | legacy |
| `ON_FINAL_RESULT` | 最终消息结果 | `(event, final_result)` | legacy |
| `ON_HINTS_PRODUCED` | hints handler 完成 | `(event, hints_result: HintsResult)` | **M5 amend** |
| `ON_EXCEPTION` | 异常 | `(event, exc_event)` | legacy |

### 6.2 Decorator 全集

```python
# 注册类（启动时一次）
@register.prompt_block(
    name, depth=100, role="system", enabled=True, cache_key=None,
    position="system", inject_depth=0, inject_role="system",   # post-amendment
)

@register.hints_handler(
    key_type, ttl_seconds=None,
    mode="every", interval=1, probability=1.0, seed=None,       # post-amendment
)

# 事件钩子类（每次触发都跑）
@on.im_message(priority=Priority.MEDIUM)
@on.message_buffered(priority=Priority.MEDIUM)
@on.im_batch_message(priority=Priority.MEDIUM)
@on.llm_request(priority=Priority.MEDIUM)
@on.prompt_assemble(priority=Priority.MEDIUM)              # M3
@on.llm_response(priority=Priority.MEDIUM)
@on.after_llm_response_parse(priority=Priority.MEDIUM)     # M5
@on.after_xml_parse(priority=Priority.MEDIUM)
@on.output_pipeline(priority=Priority.MEDIUM)              # M7
@on.tool_result(priority=Priority.MEDIUM)
@on.step_result(priority=Priority.MEDIUM)
@on.final_result(priority=Priority.MEDIUM)
@on.hints_produced(priority=Priority.MEDIUM)               # M5 amend
@on.exception(priority=Priority.MEDIUM)
```

### 6.3 PluginContext 暴露面

Phase 0 新增字段（`core/plugin/plugin_context.py`）：

```python
class PluginContext:
    # M1
    model_group_mgr: ModelGroupManager
    def get_model_group(self, group_id: str) -> Optional[ModelGroup]: ...
    
    # M3
    prompt_block_registry: PromptBlockRegistry
    
    # M5
    hints_pipeline: Optional[HintsPipeline]
```

### 6.4 配置文件清单

项目惯例（决策 #1）：所有新增配置**独立 JSON**，不再往 `kira_config` 主文件塞。统一走 `KiraConfig.load_subconfig(name) / save_subconfig(name, data)`。

| 文件 | 用途 | 引入 |
|---|---|---|
| `data/config/system_config.json` | 主配置（旧） | legacy |
| `data/config/bot_config.json` | bot 行为参数（旧） | legacy |
| `data/config/model_groups.json` | 模型组定义 | M1 |
| `data/config/translator.json` | 模板路径 + debug_markers 开关 | M4 |

### 6.5 DEPRECATED 项

下面这些**保留可用**至 Phase 1 整体清理：

| 项 | 位置 | 替代品 |
|---|---|---|
| `LLMRequest.system_prompt` 字段 | `core/provider/llm_model.py:18-21` | Assembler 产 `__assembled__` Prompt 插入 system_prompt[0] |
| `LLMRequest.user_prompt` 字段 | 同上 | message_manager 自动追加用户消息 |
| `LLMRequest.assemble_prompt()` 方法 | `core/provider/llm_model.py:42` | message_manager 直接构造 messages |
| `core/prompts/agent_tmpl.py` | 全文件 | `core/prompts/translator/*.j2` |
| `PromptBlock.role="user"` + `position="system"` | dead 路径 | `position="in_chat"` |
| `AssembledPrompt.user_blocks` | 仍填但不消费 | `chat_injections` |
| `RenderedPrompt.user_text` | 仍填但不消费 | `chat_injections` |
| `core/prompts/agent.txt` / `format.txt` | 无人引用的历史模板 | translator/*.j2 |

每条都加了 `# DEPRECATED(Phase 0 → Phase 1)` 注释。

---

## 7. 测试基础设施

### 7.1 stub 加载机制

每个测试都用同一套 `_load_from_copy_if_missing(mod_name, copy_path)` 模式：

```python
def _load_from_copy_if_missing(mod_name, copy_relpath):
    real_path = ROOT / "core" / Path(*mod_name.split(".")).with_suffix(".py")
    if real_path.exists():
        return  # core 已合并 → normal import 接管
    copy_path = ROOT / "copy" / copy_relpath
    if not copy_path.exists():
        raise RuntimeError(...)
    # 从 copy/ 目录加载并注册到 sys.modules["core.<mod>"]
    spec = importlib.util.spec_from_file_location(f"core.{mod_name}", copy_path)
    module = importlib.util.module_from_spec(spec)
    sys.modules[f"core.{mod_name}"] = module
    spec.loader.exec_module(module)
```

允许测试在**用户合并 copy/ → core/ 之前**就跑通（动态从 copy/ 拿源码）。合并后 normal import 自动接管。

部分测试（test_hints_pipeline_m5.py 的 amendment 部分）还会**检查 core/ 现有文件是否含目标符号**——例如 `ON_HINTS_PRODUCED` 是否在 plugin_handlers.py 里——若没有则用 copy/ 版本 shadow，保证 amendment 测试在合并前后都能跑。

### 7.2 各测试覆盖

| 测试文件 | 场景数 | 覆盖 |
|---|---|---|
| `tests/test_model_group_m2.py` | 8 组 27 check | priority / 429 / timeout / validate / capability / 全冷却 / __default__ / 并发 |
| `tests/test_prompt_block_m3.py` | 16 场景 | 空块、同步异步、condition、depth、cache_key、异常隔离、message_types、registry、collector、零参 provider、role="user"、in-chat × 4 |
| `tests/test_translator_m4.py` | 12 场景 | 单模板、嵌套、缺失变量、message_types 注入、缺失模板、order、debug_markers、autoescape、newline、list_templates、user_blocks、chat_injections 透传 |
| `tests/test_hints_pipeline_m5.py` | 24 场景 | 空 hints / 单 / 多合并 / 多 type / 慢 / 异常 / TTL / 覆盖 / session 隔离 / 未注册 / None / clear_session / unregister / clear_plugin / 引号容忍 + every / interval / random / probability=0 / set_policy / observer × 3 / list_pending |
| `tests/test_output_pipeline_m7.py` | (M7 文件) | 默认 delay 不变、修改 delays/chains、intercepted、budget/api 实测、调试钩子 |

### 7.3 跑测试

```powershell
# Windows 推荐用 venv 的 python
.\venv\Scripts\python.exe tests\test_model_group_m2.py
.\venv\Scripts\python.exe tests\test_prompt_block_m3.py
.\venv\Scripts\python.exe tests\test_translator_m4.py        # 需 Jinja2>=3.1.0
.\venv\Scripts\python.exe tests\test_hints_pipeline_m5.py
.\venv\Scripts\python.exe tests\test_output_pipeline_m7.py
```

不需要数据库 / 网络模型 / 实际 LLM key。每个测试独立 stub。

---

## 8. 合并状态自检

进入下一个上下文 / 接手别人的工作时第一件事：

```powershell
cd D:\KiraAI-2.12.0
git status
git log --oneline -10

# 检查 core/ 是否已合并各 milestone
ls core\provider\model_group.py        # M1+M2
ls core\prompt_block.py                # M3
ls core\prompt_assembler.py            # M3
ls core\prompt_translator.py           # M4
ls core\prompts\translator             # M4 j2 目录
ls core\pipeline\hints_pipeline.py     # M5
ls core\output\output_ctx.py           # M7
```

若都在 → core 已合并；若有缺失 → 从 `copy/<milestone>/` 拿源码起步。

`copy/` 目录按 milestone 分层叠加：M3 是地基，M4 在 M3 之上，M5 在 M3+M4 之上，M7 在 M3+M4+M5 之上（M7 也包含原始侵入式修改的回迁文件）。合并时**按 M3 → M4 → M5 → M7 顺序**逐层合，避免覆盖更新。

---

## 9. 关键陷阱合集（动手前过一遍）

### 9.1 跨模块通用

1. **`ON_LLM_REQUEST` 老钩子改 `request.user_prompt[i].content` 仍生效**——Assembler 在它之后跑，Assembler 产文本插 `system_prompt[0]`，老钩子加的 Prompt 落在后面。
2. **`request.assemble_prompt()` 是带状态的破坏性操作**——重复调用会重新 pop messages[0]、重新 insert system。M3 改造后只调一次。
3. **session_lock 持锁路径**：`send_xml_messages` 持锁；M7 输出钩子链整体在锁内；锁内长 sleep 阻塞同 session 后续消息。

### 9.2 PromptBlock / Assembler

4. **degraded path 不消费 in_chat / chat_injections**——assembler 没注入时（早期 boot / 测试 stub）所有 in_chat 块静默丢弃。
5. **stable sort 副作用**——同 depth 按注册顺序。框架内置占 10/20/.../110；插件用 5/15/.../95（注意 95 已被 skills 占）。
6. **`ON_PROMPT_ASSEMBLE` 钩子里注册的 tag 不进 `message_types`**——tag 注册属 `ON_LLM_REQUEST` 阶段。

### 9.3 Translator

7. **`debug_markers=true` 增加 token**——生产关闭。
8. **不要跨模板 `{% include %}`**——每个 j2 独立。

### 9.4 Hints Pipeline

9. **`<hints_key>` 必须放 `<msg>` 之外**——放里面会被当消息内容发出去。
10. **handler `_frequency_policy` 与装饰器二选一**——别两边都设。
11. **observer fire-and-forget + 进程关停**——副作用必须持久化的 observer 自己 try CancelledError。
12. **counter 仅在内存**——进程重启清零。
13. **kira-ai XML 修复钩子优先级**——HintsPipeline 必须在它之后跑（用 `AFTER_LLM_RESPONSE_PARSE` 而非 `ON_LLM_RESPONSE` 避免抢位）。

### 9.5 Output Pipeline (M7)

14. **chains/delays 长度对齐**——拆/合并时 delays 跟随，框架最后会用 None 补齐让 DefaultDelayHook 填。
15. **拦截后 ON_STEP_RESULT 仍触发**——`message_results=[]`，下游 handler 要容忍。
16. **delay > 5s 不要在 M7 内做**——锁内 sleep；留给 Phase 5 的独立 scheduler。

### 9.6 模型组

17. **`max_timeout` = 临时 5min**——连续 3 次升级 30min。
18. **响应校验失败默认不冷却**——可能是 prompt 锅；连续 3 次同模型才升级。
19. **进程重启冷却清零**——有意为之。

---

## 10. 设计原则

1. **追加式改造**：每个 milestone 都不破坏老插件。`@on.llm_request` / `LLMRequest.assemble_prompt` / 老 sticker 路径全部保留可用至 Phase 1。
2. **可调试 > 可扩展**：每个关键决策点写一行结构化日志（变量更新、模型组切换、输出钩子拦截）；能 grep 出"为什么这条消息没发"比"热插拔"重要。
3. **LLM 调用是稀缺资源**：能用规则 / 映射表 / 模板做的不调 LLM。旁路 LLM 走模型组自动选最便宜的；不搞 Generator→Critic→Rewriter 三步对抗。
4. **一个人开发，不搞分布式**：进程内 pub/sub + SQLite/JSON。不要 Event Sourcing、Kafka、schema versioning。
5. **新配置一律独立 JSON**：不往 `kira_config` 主文件塞。统一走 `KiraConfig.load_subconfig`。
6. **API 本底延迟优先**：所有拟人延迟 = `max(目标延迟 - 实际API耗时, 0)`，已经卡了就别再加。M7 的 `budget_ms` / `api_elapsed_ms` 就是为这个准备的。

---

## 附录 A：合并自检命令清单

```powershell
# 1. 各 milestone 文件就位
ls core\provider\model_group.py
ls core\prompt_block.py
ls core\prompt_assembler.py
ls core\prompt_translator.py
ls core\prompts\translator | Measure-Object -Line
ls core\pipeline\hints_pipeline.py
ls core\output\output_ctx.py

# 2. EventType 全部存在
Select-String -Path core\plugin\plugin_handlers.py `
              -Pattern "ON_PROMPT_ASSEMBLE|AFTER_LLM_RESPONSE_PARSE|ON_OUTPUT_PIPELINE|ON_HINTS_PRODUCED"
# 应输出 4 行

# 3. 装饰器全部存在
Select-String -Path core\plugin\plugin_registry.py `
              -Pattern "def prompt_block|def hints_handler|def prompt_assemble|def output_pipeline|def hints_produced|def after_llm_response_parse"
# 应输出 6 行

# 4. PluginContext 暴露面
Select-String -Path core\plugin\plugin_context.py `
              -Pattern "model_group_mgr|prompt_block_registry|hints_pipeline"
# 应输出 3 行

# 5. 全部测试通过
.\venv\Scripts\python.exe tests\test_model_group_m2.py
.\venv\Scripts\python.exe tests\test_prompt_block_m3.py
.\venv\Scripts\python.exe tests\test_translator_m4.py
.\venv\Scripts\python.exe tests\test_hints_pipeline_m5.py
.\venv\Scripts\python.exe tests\test_output_pipeline_m7.py
```

---

## 附录 B：插件迁移清单（如有自写插件需要迁过来）

| 老用法 | 新推荐用法 | 是否仍可用 |
|---|---|---|
| `@on.llm_request` 改 `request.system_prompt` | `@register.prompt_block` 静态注册 / `@on.prompt_assemble` 动态贡献 | ✅ |
| `request.user_prompt[i].content` mutate | 同上（用 PromptBlock） | ✅ |
| 直接 `llm_model.chat(...)` | `ctx.get_model_group(gid).call(...)` | ✅ |
| sticker "全量列在 prompt" 风格的 tag description | 走 `@register.hints_handler` + `<hints_key type="X">` | ✅（默认仍走老路径） |
| `random.uniform(min,max)` 自写消息延迟 | `@on.output_pipeline` 设 `ctx.delays` | ✅（DefaultDelayHook 默认接管） |
| 自写"发完消息后改情绪 / 写日志" | `@on.step_result` 或 `@on.output_pipeline` | ✅ |

---

> 本文档随 Phase 0 完工冻结。Phase 1+ 的更新计划见 [`计划.md`](计划.md)，与本文不耦合。
